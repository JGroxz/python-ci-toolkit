name: "Publish Poetry package"

on:
  push:
    tags:
      - 'v*'

jobs:
  publish-poetry-package:
    name: Publish Poetry package
    uses: pyci/.github/.github/workflows/publish-poetry-package.yml@main
    secrets: inherit
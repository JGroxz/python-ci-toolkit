[styles]

# rich_click
option = bold cyan
argument = bold cyan
switch = bold green
metavar = bold yellow
metavar_sep = dim yellow
usage = dim

# CI Toolkit
pyci.info = blue
pyci.success = green
pyci.error = red
pyci.flair = green
pyci.action = rgb(146,202,85)
pyci.query = yellow
pyci.search_highlight = yellow underline
pyci.command = yellow

# Regex to search for Rich markup style tags: ["'].*\[[a-z\.]*\]